使用MGET/multiGet或者 pipeline能够将多次的缓存操作合并到一次网络请求中，大大减少网络开销。

redis集群容量，跟分片数、节点总数正相关；
redis cluster集群，分片总数通常不超过100；节点总数不超过500。
单节点采用2C40G配置，提升CPU核心数作用不大。

分片越多的弊端一个是集群本身的协议消耗，另外一个是对于批量操作（MGet）的耗时会有所增加（请求节点数变多）。
分片过多会导致 Gossip 协议带宽消耗增加、选举冲突、扩缩容变慢等问题。

prod 50个分片，每个分片1主5从。